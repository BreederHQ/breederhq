# Local development reverse proxy for subdomain-based routing
# Requires Caddy installed: https://caddyserver.com/docs/install

app.breederhq.test {
	reverse_proxy localhost:6170
}

portal.breederhq.test {
	reverse_proxy localhost:6171
}

marketplace.breederhq.test {
	reverse_proxy localhost:6172
}
